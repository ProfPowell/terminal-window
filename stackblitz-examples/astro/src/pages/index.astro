---
// Astro + Terminal Window Demo
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Terminal Window - Astro Demo</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #1a1a2e;
      min-height: 100vh;
      padding: 2rem;
    }
    .container { max-width: 800px; margin: 0 auto; }
    h1 { color: #fff; margin-bottom: 1rem; }
    p { color: #888; margin-bottom: 1.5rem; }
    code { color: #ff79c6; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Terminal Window + Astro</h1>
    <p>
      A web component terminal running in an Astro site. Type
      <code>demo</code> to see available commands.
    </p>
    <terminal-window
      id="terminal"
      theme="dark"
      prompt="astro> "
      welcome="Astro + Terminal Window Demo. Type 'demo' for commands."
      cursor-style="block"
      cursor-blink
      autofocus
    />
  </div>

  <script>
    import 'terminal-window';

    const terminal = document.getElementById('terminal');

    customElements.whenDefined('terminal-window').then(() => {
      // Register custom commands
      terminal.registerCommand('astro', () => {
        return 'Hello from Astro! Perfect for static sites.';
      });

      terminal.registerCommand('static', () => {
        return 'No hydration needed for static content!';
      });

      terminal.registerCommand('island', () => {
        return 'Use client:load for interactive islands.';
      });

      terminal.registerCommand('demo', () => {
        return 'Try these commands: astro, static, island, help';
      });
    });
  </script>
</body>
</html>
